{
  "name": "migrid-core",
  "version": "10.0.0",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "docker-compose up",
    "seed": "node scripts/seed-physics.js",
    "test": "jest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "lerna": "^7.0.0",
    "typescript": "^5.0.0"
  }
}
